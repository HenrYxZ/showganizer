<%= form_for([@season,@photo_season]) do |f| %>
  <% if @photo_season.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@photo_season.errors.count, "error") %> prohibited this photo_series from being saved:</h2>

      <ul>
      <% @photo_season.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :data, "Image" %><br>
    <%= f.file_field  :data %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

