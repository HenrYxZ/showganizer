<%= form_for([@series,@photo_series]) do |f| %>
  <% if @photo_series.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@photo_series.errors.count, "error") %> prohibited this photo_series from being saved:</h2>

      <ul>
      <% @photo_series.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :data %><br>
    <%= f.file_field  :data %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
