
<%= render "series_list"%>

<div class="container-button">
  <%if current_user_is_admin?%>
    <%= link_to 'New series', new_polymorphic_path([@parent, :series]), class: "button add" %>
  <%end%>
</div>