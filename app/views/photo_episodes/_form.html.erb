<%= form_for([@parent,@photo_episode]) do |f| %>
  <% if @photo_episode.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@photo_episode.errors.count, "error") %> prohibited this photo_episode from being saved:</h2>

      <ul>
      <% @photo_episode.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :data %><br>
    <%= f.file_field  :data %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>